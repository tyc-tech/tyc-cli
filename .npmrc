# 启用 legacy peer deps 解析模式
# 原因：@typescript-eslint/* 系列对 typescript / eslint 的 peer 范围声明严格，
# npm 10+ 默认 strict 模式会卡住。设为 true 即可让 npm 沿用旧策略，
# typescript 5.7 与 eslint 9.x 实际完全兼容。
legacy-peer-deps=true
